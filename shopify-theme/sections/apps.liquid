{% if section.blocks.size > 0 %}
  <section class="t-apps">
    {% for block in section.blocks %}
      {% render block %}
    {% endfor %}
  </section>
{% endif %}

{% schema %}
{
  "name": "App integrations",
  "settings": [],
  "blocks": [
    {"type": "@app"}
  ],
  "presets": [
    {"name": "App integrations"}
  ]
}
{% endschema %}
